// App helper tests moved to src/lib/__tests__/save-utils.test.ts
// This file kept as a placeholder — App.tsx is the wiring layer
// and is covered by the component + lib tests.
import { describe, it, expect } from "vitest";

describe("App", () => {
  it("is the wiring layer (tested via components and lib)", () => {
    expect(true).toBe(true);
  });
});
